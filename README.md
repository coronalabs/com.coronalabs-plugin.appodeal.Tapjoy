Sources for the plugin `plugin.appodeal.Tapjoy`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.Tapjoy" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
